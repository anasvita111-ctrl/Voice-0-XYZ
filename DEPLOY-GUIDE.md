# دليل نشر تطبيق إحداثيات الصوت على Vercel
## خطوات بسيطة — لا تحتاج أي خبرة برمجية

---

## الخطوة 1 — إنشاء حساب GitHub (مجاني)
1. اذهب إلى https://github.com
2. اضغط "Sign up"
3. أدخل بريدك الإلكتروني وكلمة مرور واسم مستخدم
4. فعّل الحساب من بريدك

---

## الخطوة 2 — رفع ملفات التطبيق على GitHub
1. بعد تسجيل الدخول، اضغط الزر الأخضر "New" لإنشاء مستودع جديد
2. سمّه: `voice-coordinates`
3. اجعله **Public**
4. اضغط "Create repository"
5. اضغط "uploading an existing file"
6. ارفع هذه الملفات الأربعة:
   - `index.html`
   - `app.jsx`
   - `sw.js`
   - `manifest.json`
   - `vercel.json`
   - `icon-192.svg`
   - `icon-512.svg`
7. اضغط "Commit changes"

---

## الخطوة 3 — نشر على Vercel (مجاني)
1. اذهب إلى https://vercel.com
2. اضغط "Sign up" وسجّل **بنفس حساب GitHub**
3. اضغط "Add New Project"
4. ستجد مستودع `voice-coordinates` — اضغط "Import"
5. اضغط "Deploy" مباشرة بدون أي تغييرات
6. انتظر دقيقة واحدة ✅

---

## النتيجة
ستحصل على رابط مثل:
**`https://voice-coordinates.vercel.app`**

شارك هذا الرابط مع أي شخص!

---

## كيف يثبّته المستخدم على iPhone
1. يفتح الرابط في **Safari** (مهم — ليس Chrome)
2. يضغط زر المشاركة ↑ في الأسفل
3. يختار "إضافة إلى الشاشة الرئيسية"
4. يضغط "إضافة"
5. التطبيق يظهر على شاشته مثل أي تطبيق عادي! 📱

---

## ملاحظات مهمة
- التطبيق **مجاني 100%** على Vercel لحد 100GB شهرياً
- يعمل على iPhone وAndroid وأي متصفح
- يعمل **بدون إنترنت** بعد أول زيارة (Service Worker)
- لتحديث التطبيق: عدّل الملفات على GitHub وسيُحدَّث تلقائياً
